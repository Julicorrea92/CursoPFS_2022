/*Teniendo en cuenta las siguientes afirmaciones:

1) La sumatoria de los primeros N numeros naturales se calcula como (N*(N+1))/2
2) La sumatoria de numeros naturales sucesivos entre P y Q se calcula como ((P+Q)*(Q-P-1))/2
3) La sumatoria del cuadrado de los primeros N numeros naturales se calcula como (N*(N+1)*((2*N)+1))

*/

import * as rls from 'readline-sync';


/* Despues de probar en papel si los enunciados eran correctos, tenía pensado hacer una función que ejecutara
la suma de los calculos algebráicos y la comparara con otra función para enunciado y me devolviera si es verdadero o falso.





/*let limite: number= (rls.questionInt('Ingresar la cantidad de numeros: '));

let arregloN: number [] = new Array (limite);

let i:number=0;

for (i=0; i < limite; i ++ ){

    arregloN [i]= sumarRecta(limite)

}
let sumatoria: number =0;

for (let i=0; i < numNaturales; i ++ ){

    sumatoria += arregloN[i];
    
//console.log(`Los numero ingresados son ${arregloN[i]}`)
console.log(`La sumatoria de numeros sucesivos es: ${sumatoria}`);
}

function sumarRecta (limite: number): number{




return



}



*/



















